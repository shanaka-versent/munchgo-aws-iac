# Kong Cloud Gateway on EKS - TLS Secret Template
# @author Shanaka Jayasundera - shanakaj@gmail.com
#
# The TLS secret is created automatically by the cert generation script:
#   ./scripts/01-generate-certs.sh
#
# The script generates a self-signed CA + server certificate and creates
# the 'istio-gateway-tls' secret in the istio-ingress namespace.
#
# This secret is referenced by the Istio Gateway HTTPS listener (port 443)
# for TLS termination. See: k8s/istio/gateway.yaml

---
# Template only - created automatically by ./scripts/01-generate-certs.sh
# apiVersion: v1
# kind: Secret
# metadata:
#   name: istio-gateway-tls
#   namespace: istio-ingress
# type: kubernetes.io/tls
# data:
#   tls.crt: <base64-encoded-certificate>
#   tls.key: <base64-encoded-key>
