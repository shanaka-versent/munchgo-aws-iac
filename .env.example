# Kong Konnect Cloud Gateway on EKS - Environment Variables
# @author Shanaka Jayasundera - shanakaj@gmail.com
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# .env is gitignored and will NEVER be committed.
# All scripts automatically source .env if it exists.

# ==============================================================================
# KONG KONNECT CREDENTIALS (Required)
# ==============================================================================

# Konnect region: us, eu, au
KONNECT_REGION="au"

# Personal Access Token from Konnect
# Generate at: https://cloud.konghq.com → Settings → Personal Access Tokens
KONNECT_TOKEN="kpat_your_token_here"

# Control Plane name (used by decK and setup script)
KONNECT_CONTROL_PLANE_NAME="kong-cloud-gateway-eks"

# ==============================================================================
# TRANSIT GATEWAY (Auto-populated from Terraform — no manual entry needed)
# ==============================================================================
# Scripts automatically read these from 'terraform output'.
# Only set these manually if running scripts outside the repo directory.
#
# TRANSIT_GATEWAY_ID="tgw-xxxxxxxxx"
# RAM_SHARE_ARN="arn:aws:ram:ap-southeast-2:ACCOUNT_ID:resource-share/xxxxxxxx"
# EKS_VPC_CIDR="10.0.0.0/16"

# ==============================================================================
# ISTIO GATEWAY NLB (Auto-discovered by scripts — no manual entry needed)
# ==============================================================================
# Run ./scripts/03-post-terraform-setup.sh to discover and display the NLB DNS.
# Used when updating deck/kong.yaml service URLs.
#
# ISTIO_GATEWAY_NLB_DNS="k8s-istioing-xxxxxxxx.elb.ap-southeast-2.amazonaws.com"

# ==============================================================================
# CLOUDFRONT + WAF (Custom origin header for bypass prevention)
# ==============================================================================
# Custom origin header for CloudFront bypass prevention
# Must match cf_origin_header_value in terraform.tfvars
#
# CF_ORIGIN_HEADER_VALUE="your-secret-value"
